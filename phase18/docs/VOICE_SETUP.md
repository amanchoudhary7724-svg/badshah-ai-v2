# Voice Setup

If PyAudio fails:

```bat
pip install pipwin
pipwin install pyaudio
```

Run voice:

```bat
installer\START_VOICE_MODE.bat
```

Say:

```text
badshah help
badshah create website portfolio
badshah exit
```
